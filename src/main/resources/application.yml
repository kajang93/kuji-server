spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kuji_db
    username: kuji_user
    password: kuji2025local!
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
  address: 0.0.0.0
